import { FilterStockedPipe } from './filter-stocked.pipe';

describe('FilterStockedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStockedPipe();
    expect(pipe).toBeTruthy();
  });
});
